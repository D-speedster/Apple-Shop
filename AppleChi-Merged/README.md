# 🍎 فروشگاه AppleChi

یک فروشگاه آنلاین کامل برای فروش محصولات اپل با Next.js

![Next.js](https://img.shields.io/badge/Next.js-14.2.33-black?style=flat-square&logo=next.js)
![React](https://img.shields.io/badge/React-18-blue?style=flat-square&logo=react)
![MongoDB](https://img.shields.io/badge/MongoDB-8.2.2-green?style=flat-square&logo=mongodb)

---

## 📖 فهرست مطالب

- [درباره پروژه](#-درباره-پروژه)
- [ویژگی‌ها](#-ویژگیها)
- [تکنولوژی‌ها](#-تکنولوژیها)
- [پیش‌نیازها](#-پیشنیازها)
- [نصب و راه‌اندازی](#-نصب-و-راهاندازی)
- [ساختار پروژه](#-ساختار-پروژه)
- [API Documentation](#-api-documentation)
- [راهنمای توسعه](#-راهنمای-توسعه)
- [مشکلات رایج](#-مشکلات-رایج)
- [دیپلوی](#-دیپلوی)

---

## 🎯 درباره پروژه

این پروژه یک فروشگاه آنلاین کامل برای فروش محصولات اپل است که شامل:
- **سایت اصلی**: برای نمایش و فروش محصولات
- **پنل ادمین**: برای مدیریت محصولات، کاربران و کامنت‌ها

### چرا این پروژه مناسب فرانت‌اند جونیور است؟

✅ کد تمیز و خوانا  
✅ ساختار منظم و استاندارد  
✅ استفاده از تکنولوژی‌های محبوب  
✅ مستندات کامل  
✅ مثال‌های واقعی از CRUD operations  
✅ کار با API و دیتابیس  

---

## ✨ ویژگی‌ها

### سایت اصلی 🛍️

- 🏠 صفحه اصلی با اسلایدر جذاب
- 📱 نمایش محصولات بر اساس دسته‌بندی (گوشی، مک، واچ)
- 🔍 صفحه جزئیات محصول با گالری تصاویر
- 📰 بخش بلاگ
- 📱 طراحی ریسپانسیو (موبایل، تبلت، دسکتاپ)
- 🎨 استفاده از Bootstrap 5

### پنل ادمین 👨‍💼

- 📊 داشبورد با نمودارها و آمار
- 📦 مدیریت محصولات (افزودن، ویرایش، حذف)
- 👥 مدیریت کاربران
- 💬 مدیریت کامنت‌ها با فیلتر
- 🎨 طراحی Dark Mode
- 📈 نمودارهای تعاملی با Chart.js

---

## 🛠 تکنولوژی‌ها

### Frontend
- **Next.js 14** - فریمورک React
- **React 18** - کتابخانه UI
- **Bootstrap 5** - فریمورک CSS
- **Swiper** - اسلایدر تصاویر
- **Chart.js** - نمودارها

### Backend
- **Next.js API Routes** - API
- **MongoDB** - دیتابیس
- **Mongoose** - ODM برای MongoDB

### دیگر ابزارها
- **SweetAlert2** - نمایش پیام‌های زیبا
- **Material-UI** - کامپوننت‌های UI
- **AWS SDK** - آپلود تصاویر

---

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شو این‌ها رو نصب کردی:

### نرم‌افزارهای مورد نیاز:

1. **Node.js** (نسخه 14 یا بالاتر)
   ```bash
   # چک کردن نسخه
   node --version
   ```

2. **MongoDB** (نسخه 4 یا بالاتر)
   ```bash
   # چک کردن نسخه
   mongod --version
   ```

3. **npm** یا **yarn**
   ```bash
   # چک کردن نسخه
   npm --version
   ```

### دانلود و نصب:

- **Node.js**: [nodejs.org](https://nodejs.org/)
- **MongoDB**: [mongodb.com/try/download/community](https://www.mongodb.com/try/download/community)
- **Git**: [git-scm.com](https://git-scm.com/)

---

## 🚀 نصب و راه‌اندازی

### مرحله 1: دانلود پروژه

```bash
# کلون کردن پروژه
git clone https://github.com/your-username/applechi-merged.git

# رفتن به پوشه پروژه
cd AppleChi-Merged
```

### مرحله 2: نصب پکیج‌ها

```bash
npm install
```

این دستور تمام پکیج‌های مورد نیاز رو نصب می‌کنه (ممکنه 2-3 دقیقه طول بکشه).

### مرحله 3: تنظیم دیتابیس

#### روشن کردن MongoDB:

**Windows:**
```bash
net start MongoDB
```

**Mac/Linux:**
```bash
sudo systemctl start mongod
```

#### چک کردن اتصال:
```bash
# باز کردن MongoDB Shell
mongosh

# اگه وصل شد، این رو می‌بینی:
# Current Mongosh Log ID: ...
```

### مرحله 4: تنظیم Environment Variables

فایل `.env.local` از قبل آماده است، ولی می‌تونی تغییرش بدی:

```env
MONGODB_URI=mongodb://127.0.0.1:27017/AppleChi
LIARA_ENDPOINT=https://storage.iran.liara.space
LIARA_BUCKET_NAME=apple
LIARA_ACCESS_KEY=your_access_key
LIARA_SECRET_KEY=your_secret_key
```

### مرحله 5: اجرای پروژه

```bash
npm run dev
```

پروژه روی `http://localhost:3000` اجرا می‌شه! 🎉

---

## 📂 ساختار پروژه

```
AppleChi-Merged/
│
├── 📁 components/          # کامپوننت‌های React
│   ├── 📁 admin/          # کامپوننت‌های پنل ادمین
│   │   ├── ChartBar.js    # نمودار میله‌ای
│   │   ├── ChartLine.js   # نمودار خطی
│   │   ├── Comment.js     # آیتم کامنت
│   │   ├── HeaderAdmin.js # هدر ادمین
│   │   ├── SideBar.js     # منوی کناری
│   │   ├── TitlePage.js   # عنوان صفحه
│   │   └── User.js        # آیتم کاربر
│   │
│   ├── 📁 layout/         # لی‌اوت‌ها
│   │   ├── AdminLayout.js # لی‌اوت پنل ادمین
│   │   └── MainLayout.js  # لی‌اوت سایت اصلی
│   │
│   └── 📁 site/           # کامپوننت‌های سایت
│       ├── Ads.js         # بنرهای تبلیغاتی
│       ├── Blog.js        # بخش بلاگ
│       ├── CategoryStore.js # دسته‌بندی‌ها
│       ├── Footer.js      # فوتر
│       ├── Header.js      # هدر
│       ├── HeaderSlider.js # اسلایدر اصلی
│       └── ProductSection.js # بخش محصولات
│
├── 📁 pages/              # صفحات Next.js
│   ├── 📁 admin/          # صفحات پنل ادمین
│   │   ├── 📁 comments/   # مدیریت کامنت‌ها
│   │   ├── 📁 products/   # مدیریت محصولات
│   │   ├── 📁 users/      # مدیریت کاربران
│   │   └── index.js       # داشبورد
│   │
│   ├── 📁 api/            # API Routes
│   │   └── 📁 products/   # API محصولات
│   │       ├── [id].js    # دریافت یک محصول
│   │       └── index.js   # CRUD محصولات
│   │
│   ├── 📁 products/       # صفحات محصولات
│   │   └── [id].js        # جزئیات محصول
│   │
│   ├── _app.js            # تنظیمات اصلی Next.js
│   └── index.js           # صفحه اصلی
│
├── 📁 models/             # مدل‌های دیتابیس
│   └── Product.js         # مدل محصول
│
├── 📁 utils/              # توابع کمکی
│   └── db.js              # اتصال به دیتابیس
│
├── 📁 styles/             # فایل‌های CSS
│   ├── admin.css          # استایل پنل ادمین
│   └── globals.css        # استایل عمومی
│
├── 📁 public/             # فایل‌های استاتیک
│   ├── 📁 img/            # تصاویر
│   ├── 📁 IRANSans/       # فونت فارسی
│   └── admin.css          # استایل ادمین (کپی)
│
├── .env.local             # متغیرهای محیطی
├── .gitignore             # فایل‌های نادیده گرفته شده
├── jsconfig.json          # تنظیمات JavaScript
├── package.json           # پکیج‌ها و اسکریپت‌ها
└── README.md              # این فایل!
```

---

## 🔌 API Documentation

### محصولات (Products)

#### دریافت همه محصولات
```http
GET /api/products
```

**پاسخ:**
```json
{
  "success": true,
  "data": [
    {
      "_id": "123",
      "title": "آیفون 15 پرو",
      "price": 45000000,
      "category": "گوشی",
      "type": "phone",
      "IMG": "/img/iphone15.png"
    }
  ]
}
```

#### دریافت یک محصول
```http
GET /api/products/[id]
```

#### ایجاد محصول جدید
```http
POST /api/products
Content-Type: application/json

{
  "title": "آیفون 15 پرو",
  "price": 45000000,
  "category": "گوشی",
  "type": "phone",
  "cpu": "A17 Pro",
  "ram": "8GB"
}
```

#### ویرایش محصول
```http
PUT /api/products
Content-Type: application/json

{
  "id": "123",
  "title": "آیفون 15 پرو مکس",
  "price": 50000000
}
```

#### حذف محصول
```http
DELETE /api/products
Content-Type: application/json

{
  "id": "123"
}
```

---

## 💻 راهنمای توسعه

### اضافه کردن صفحه جدید

1. فایل جدید در `pages/` بساز:
```javascript
// pages/about.js
export default function About() {
  return <h1>درباره ما</h1>
}
```

2. صفحه خودکار در `/about` در دسترس می‌شه!

### اضافه کردن کامپوننت جدید

1. فایل جدید در `components/` بساز:
```javascript
// components/site/NewComponent.js
export default function NewComponent() {
  return <div>کامپوننت جدید</div>
}
```

2. در صفحه مورد نظر import کن:
```javascript
import NewComponent from '@/components/site/NewComponent'
```

### کار با دیتابیس

#### ایجاد مدل جدید:
```javascript
// models/User.js
import mongoose from 'mongoose';

const userSchema = new mongoose.Schema({
  name: String,
  email: String
});

export default mongoose.models.User || mongoose.model('User', userSchema);
```

#### استفاده در API:
```javascript
import User from '@/models/User';
import connectDB from '@/utils/db';

export default async function handler(req, res) {
  await connectDB();
  const users = await User.find({});
  res.json({ users });
}
```

---

## 🐛 مشکلات رایج

### مشکل 1: MongoDB وصل نمی‌شه

**علت:** MongoDB روشن نیست.

**راه‌حل:**
```bash
# Windows
net start MongoDB

# Mac/Linux
sudo systemctl start mongod
```

### مشکل 2: پورت 3000 اشغاله

**راه‌حل:**
```bash
# اجرا روی پورت دیگه
npm run dev -- -p 3001
```

### مشکل 3: تصاویر نمایش داده نمی‌شن

**راه‌حل:**
- مطمئن شو تصاویر در `public/img/` هستن
- مسیر تصویر باید با `/img/` شروع بشه

### مشکل 4: استایل‌ها اعمال نمی‌شن

**راه‌حل:**
1. سرور رو restart کن (Ctrl+C و دوباره `npm run dev`)
2. کش مرورگر رو پاک کن (Ctrl+Shift+R)

---

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها

فایل `styles/globals.css` رو باز کن:

```css
:root {
  --primary-color: #181c2a;    /* رنگ اصلی */
  --Secondary-color: #000000;   /* رنگ ثانویه */
  --primary-Text: #FFFf;        /* رنگ متن */
}
```

### تغییر فونت

فایل `public/IRANSans/` رو با فونت دلخواهت جایگزین کن.

---

## 📦 دیپلوی

### Vercel (توصیه می‌شه)

1. پروژه رو به GitHub پوش کن
2. به [vercel.com](https://vercel.com) برو
3. "Import Project" رو بزن
4. Repository رو انتخاب کن
5. Environment Variables رو اضافه کن
6. Deploy!

### متغیرهای محیطی برای Production:

```env
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/applechi
LIARA_ENDPOINT=https://storage.iran.liara.space
LIARA_BUCKET_NAME=your-bucket
LIARA_ACCESS_KEY=your-key
LIARA_SECRET_KEY=your-secret
```

---

## 📚 منابع یادگیری

### برای یادگیری بیشتر:

- **Next.js**: [nextjs.org/learn](https://nextjs.org/learn)
- **React**: [react.dev](https://react.dev)
- **MongoDB**: [mongodb.com/docs](https://www.mongodb.com/docs/)
- **Bootstrap**: [getbootstrap.com](https://getbootstrap.com/)

### ویدیوهای آموزشی فارسی:

- آموزش Next.js در یوتیوب
- آموزش React در یوتیوب
- آموزش MongoDB در یوتیوب

---

## 🤝 مشارکت

می‌خوای در پروژه مشارکت کنی؟ عالیه!

1. پروژه رو Fork کن
2. یک Branch جدید بساز (`git checkout -b feature/amazing-feature`)
3. تغییراتت رو Commit کن (`git commit -m 'Add amazing feature'`)
4. Push کن (`git push origin feature/amazing-feature`)
5. یک Pull Request باز کن

---

## 📝 دستورات مفید

```bash
# اجرای پروژه
npm run dev

# ساخت برای production
npm run build

# اجرای production
npm start

# چک کردن خطاها
npm run lint

# نصب پکیج جدید
npm install package-name

# حذف node_modules و نصب مجدد
rm -rf node_modules
npm install
```

---

## 🎓 یادگیری از این پروژه

### چیزهایی که یاد می‌گیری:

✅ **Next.js Routing** - چطور صفحات بسازی  
✅ **React Components** - کامپوننت‌سازی  
✅ **API Routes** - ساخت API  
✅ **MongoDB** - کار با دیتابیس  
✅ **State Management** - مدیریت state  
✅ **Responsive Design** - طراحی ریسپانسیو  
✅ **CRUD Operations** - عملیات پایه دیتابیس  

---

## 📞 پشتیبانی

اگه سوال یا مشکلی داشتی:

1. **مستندات**: این README رو بخون
2. **Issues**: در GitHub یک Issue باز کن
3. **Email**: your-email@example.com

---

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده - فایل [LICENSE](LICENSE) رو ببین.

---

## 🙏 تشکر

از این پروژه‌ها و ابزارها استفاده شده:

- [Next.js](https://nextjs.org/)
- [React](https://react.dev/)
- [MongoDB](https://www.mongodb.com/)
- [Bootstrap](https://getbootstrap.com/)
- [Swiper](https://swiperjs.com/)
- [Chart.js](https://www.chartjs.org/)

---

## 🌟 نکته پایانی

این پروژه برای یادگیری و توسعه مهارت‌های فرانت‌اند عالیه!

**نکات مهم:**
- کد رو بخون و بفهم
- تغییرات کوچیک بده و تست کن
- از Git استفاده کن
- سوال بپرس!

**موفق باشی! 🚀**

---

<div align="center">

**ساخته شده با ❤️ توسط speedster**

⭐️ اگه این پروژه بهت کمک کرد، یک ستاره بده!

</div>
