# فروشگاه AppleChi - نسخه ادغام شده

یک فروشگاه آنلاین کامل با Next.js برای فروش محصولات اپل (سایت اصلی + پنل ادمین)

## ویژگی‌ها

### سایت اصلی
- نمایش محصولات با دسته‌بندی (گوشی، مک، واچ)
- صفحه جزئیات محصول با گالری تصاویر
- اسلایدر هدر
- بخش بلاگ
- طراحی ریسپانسیو

### پنل مدیریت
- داشبورد با نمودارها و آمار
- مدیریت کامل محصولات (افزودن، ویرایش، حذف)
- مدیریت کاربران
- مدیریت کامنت‌ها

## تکنولوژی‌ها

- Next.js 14
- MongoDB + Mongoose
- Bootstrap 5
- Swiper
- Chart.js
- SweetAlert2

## نصب و راه‌اندازی

### 1. نصب پکیج‌ها
```bash
npm install
```

### 2. تنظیم Environment Variables
فایل `.env.local` از قبل آماده است.

### 3. روشن کردن MongoDB
```bash
# Windows
net start MongoDB

# Mac/Linux
sudo systemctl start mongod
```

### 4. اجرای پروژه
```bash
npm run dev
```

پروژه روی `http://localhost:3000` اجرا می‌شه.

## مسیرها

### سایت اصلی
- `/` - صفحه اصلی
- `/products/[id]` - جزئیات محصول

### پنل ادمین
- `/admin` - داشبورد
- `/admin/products` - مدیریت محصولات
- `/admin/products/new` - افزودن محصول
- `/admin/users` - مدیریت کاربران
- `/admin/comments` - مدیریت کامنت‌ها

## API Endpoints

```
GET    /api/products          # دریافت همه محصولات
POST   /api/products          # ایجاد محصول جدید
PUT    /api/products          # ویرایش محصول
DELETE /api/products          # حذف محصول
GET    /api/products/[id]     # دریافت یک محصول
```

## ساختار پروژه

```
AppleChi-Merged/
├── pages/
│   ├── index.js                    # صفحه اصلی
│   ├── products/[id].js            # جزئیات محصول
│   ├── admin/                      # پنل ادمین
│   │   ├── index.js                # داشبورد
│   │   ├── products/               # مدیریت محصولات
│   │   ├── users/                  # مدیریت کاربران
│   │   └── comments/               # مدیریت کامنت‌ها
│   ├── api/products/               # API محصولات
│   └── _app.js                     # تنظیمات اصلی
├── components/
│   ├── layout/                     # لی‌اوت‌ها
│   ├── site/                       # کامپوننت‌های سایت
│   └── admin/                      # کامپوننت‌های ادمین
├── models/Product.js               # مدل محصول
├── utils/db.js                     # اتصال دیتابیس
└── styles/globals.css              # استایل‌ها
```

## دستورات مفید

```bash
# اجرای محیط توسعه
npm run dev

# ساخت برای production
npm run build

# اجرای production
npm start
```

## نکات مهم

1. MongoDB باید روشن باشه
2. پورت پیش‌فرض: 3000
3. برای تست، می‌تونی از پنل ادمین محصول اضافه کنی

## توسعه دهنده

⚡️ طراحی شده توسط speedster ⚡️

---

**موفق باشید! 🚀**
