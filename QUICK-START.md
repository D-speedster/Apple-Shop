# راهنمای سریع شروع کار

## مراحل اجرا (5 دقیقه)

### 1. نصب پکیج‌ها
```bash
npm install
```

### 2. روشن کردن MongoDB
```bash
# Windows (در CMD به عنوان Administrator)
net start MongoDB

# یا اگر نصب نکردی، از MongoDB Compass استفاده کن
```

### 3. اجرای پروژه
```bash
npm run dev
```

### 4. باز کردن مرورگر
- سایت اصلی: http://localhost:3000
- پنل ادمین: http://localhost:3000/admin

---

## اضافه کردن محصول اول

1. برو به: http://localhost:3000/admin/products
2. کلیک روی "افزودن محصول جدید"
3. فرم رو پر کن:
   - عنوان: آیفون 15 پرو
   - قیمت: 45000000
   - دسته‌بندی: گوشی
   - نوع: phone
   - پردازنده: A17 Pro
   - رم: 8GB
   - تصویر: /img/Iphone15.png
4. ذخیره کن
5. برو به صفحه اصلی و محصول رو ببین!

---

## مشکلات رایج

### MongoDB وصل نمی‌شه
```bash
# چک کن MongoDB روشنه
mongod --version

# اگه نصب نیست، نصبش کن از:
# https://www.mongodb.com/try/download/community
```

### پورت 3000 اشغاله
```bash
# پورت رو عوض کن:
npm run dev -- -p 3001
```

### تصاویر نمایش داده نمی‌شن
- مطمئن شو تصاویر در پوشه `public/img` هستن
- مسیر تصویر باید با `/img/` شروع بشه

---

## تست سریع

### تست API
```bash
# دریافت محصولات
curl http://localhost:3000/api/products
```

### تست با Postman
```
POST http://localhost:3000/api/products
Content-Type: application/json

{
  "title": "آیفون 15",
  "price": 40000000,
  "category": "گوشی",
  "type": "phone",
  "cpu": "A17",
  "ram": "6GB"
}
```

---

## مسیرهای مهم

| مسیر | توضیح |
|------|-------|
| `/` | صفحه اصلی |
| `/products/[id]` | جزئیات محصول |
| `/admin` | داشبورد ادمین |
| `/admin/products` | لیست محصولات |
| `/admin/products/new` | افزودن محصول |

---

## دستورات مفید

```bash
# اجرا
npm run dev

# ساخت
npm run build

# اجرای production
npm start

# نصب پکیج جدید
npm install package-name
```

---

**آماده‌ای؟ بزن `npm run dev` و شروع کن! 🚀**
